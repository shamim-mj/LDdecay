% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_single_snp_ld.R
\name{plot_single_snp_ld}
\alias{plot_single_snp_ld}
\title{Plot Linkage Disequilibrium Heatmap Around a Focal Variant}
\usage{
plot_single_snp_ld(
  geno,
  map,
  focal_snp,
  window_bp = 1e+05,
  marker_col = "SNP",
  chr_col = "Chromosome",
  pos_col = "Position",
  min_maf = 0.05
)
}
\arguments{
\item{geno}{A numeric matrix or data frame of genotype. Rows are samples, columns are SNPs. First col is taxa}

\item{map}{A data frame containing variant metadata map records (SNP, Chromosome, Position).}

\item{focal_snp}{Character string identifying the central target SNP (significant SNP).}

\item{window_bp}{Numeric. Distance upstream/downstream to include variants. Default is \code{100000} (100kb).}

\item{marker_col}{Character. Column name for variant IDs in the map file. Default is \code{"SNP"}. Change the SNP name to your map col name}

\item{chr_col}{Character. Column name for chromosomes in the map file. Default is \code{"Chromosome"}.}

\item{pos_col}{Character. Column name for base-pair positions in the map file. Default is \code{"Position"}.}

\item{min_maf}{Numeric. Minor Allele Frequency threshold to filter variants. Default is \code{0.05}.}
}
\value{
A list containing the \code{ggplot} object, the raw $r^2$ correlation matrix, and filtered maps/genotypes. This plot shows the correlation of the significant or focal SNP to other SNPs. Focal SNP is indicated with a "*"
}
\description{
Isolates flanking markers inside a physical window, filters out variants with 
a low Minor Allele Frequency (MAF), and renders a correlation matrix heatmap.
}
